# Observability

Prometheus scrape and alert rules (`prometheus/`), Grafana dashboards (`grafana/`) and OpenTelemetry Collector configuration (`otel/`) shared by the local Compose environment and the cluster (SPECIFICATIONS.md sections 39–43).

Not yet populated; see `docs/IMPLEMENTATION_PLAN.md`, Phase 6.
