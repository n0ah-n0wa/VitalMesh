# Terraform

AWS infrastructure as code: VPC, EKS, ECR, RDS PostgreSQL, ElastiCache Redis, IAM and logging (SPECIFICATIONS.md sections 57–59 and 100). Planned layout is `bootstrap/`, `modules/` and `environments/{staging,production}`.

Not yet populated; see `docs/IMPLEMENTATION_PLAN.md`, Phase 9.
