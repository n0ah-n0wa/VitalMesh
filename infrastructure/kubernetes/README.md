# Kubernetes

Application manifests for both services (SPECIFICATIONS.md sections 33–36 and 101–102). Planned layout is `base/` plus `overlays/{local,staging,production}`, validated on a local kind cluster before any cloud deployment.

Not yet populated; see `docs/IMPLEMENTATION_PLAN.md`, Phase 8.
