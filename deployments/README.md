# Deployments

Container build definitions (`docker/`) and the Docker Compose environment (`compose/`) that runs the complete system locally (SPECIFICATIONS.md sections 32 and 53).

Not yet populated; Dockerfiles and Compose arrive with the first service that needs PostgreSQL and Redis. See `docs/IMPLEMENTATION_PLAN.md`, Phase 2.
