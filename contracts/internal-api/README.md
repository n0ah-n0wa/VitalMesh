# Internal API contract

OpenAPI description of the internal HTTP/JSON API served under `/internal/v1` by the Rust processor and consumed by the Go gateway (SPECIFICATIONS.md section 8). It is versioned independently of the public API; incompatible changes without a version bump fail CI (section 49).

Not yet populated; see `docs/IMPLEMENTATION_PLAN.md`, Phase 1.
