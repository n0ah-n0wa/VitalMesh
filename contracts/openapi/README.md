# Public API contract

OpenAPI description of the public REST API served under `/api/v1` by the Go gateway (SPECIFICATIONS.md sections 9–13 and 24–28). The contract is written before the handlers and is linted and diffed for breaking changes in CI.

Not yet populated; see `docs/IMPLEMENTATION_PLAN.md`, Phase 1.
